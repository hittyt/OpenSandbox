This plugin only sends requests to the OpenSandbox server configured by the user.
No data is sent to any third-party services by default.

Data handled:
- User-provided parameters for tool invocation
- OpenSandbox API responses

Data storage:
- The plugin does not persist user data.
- The OpenSandbox server may store logs or data per its own configuration.
